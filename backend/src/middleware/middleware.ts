// import jwt from "jsonwebtoken";

// const isUser = async (req, res) => {
//     try{
//         const getToken = res.cookie.token;
//         const decode = jwt.decode(getToken);
//         req.body = decode.token.id;
//     }catch(error){

//     }
// }